apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Chart.Name }}-homecluster-io
spec:
  secretName: {{ .Chart.Name }}-homecluster-io-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - {{ .Chart.Name }}.homecluster.io