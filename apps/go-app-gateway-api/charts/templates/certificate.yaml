apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Chart.Name }}-654537853-xyz
spec:
  secretName: {{ .Chart.Name }}-654537853-xyz-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - {{ .Chart.Name }}.654537853.xyz